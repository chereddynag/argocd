apiVersion: v1
kind: Service
metadata:
  name: argocdapp-svc
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: argocdapp
  ports:
  - port: 80
    targetPort: 80
